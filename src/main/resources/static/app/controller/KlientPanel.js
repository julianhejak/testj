Ext.define('AM.controller.KlientPanel', {
    extend: 'Ext.app.Controller',
    views: [
         'AM.view.klient.KlientPanel',
    ],
	init: function() {
        this.control({
        	
        });
    }
});